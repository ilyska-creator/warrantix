# Valuon — Полный аудит кода (14.07.2026)

Охват: 9 HTML, 23 JS, 5 CSS, i18n, SQL-схема, конфигурация.

---

## 🔴 КРИТИЧЕСКИЕ (исправить немедленно)

| # | Описание | Файл | Строки |
|---|----------|------|--------|
| ~~**BUG-1**~~ | ~~`typeof applyBusinessTranslations` без `window.`~~ | ~~`js/business-panel.js`~~ | ~~576–578~~ |
| ~~**BUG-2**~~ | ~~`unlockScroll()` вызывается безусловно при загрузке страницы~~ | ~~`js/dashboard-nav.js`~~ | ~~71~~ |
| ~~**BUG-3**~~ | ~~`data-i18n="notif_checking"` в innerHTML — спиннер никогда не переводится~~ | ~~`js/dashboard-notifications.js`~~ | ~~42~~ |
| ~~**SEC-1**~~ | ~~`escapeHtml` не экранирует одинарные кавычки~~ | ~~`js/business-panel.js:637` / `js/dashboard-items.js:19` / `js/receipts.js:33`~~ | ~~—~~ |
| ~~**SEC-3**~~ | ~~`var(--danger)` и `var(--text-secondary)` не определены~~ | ~~`css/business.css:471` / `css/verify.css:38`~~ | ~~—~~ |
| ~~**SQL-1**~~ | ~~`verify_get_receipt` с `SECURITY DEFINER` без `set search_path`~~ | ~~SQL-функция (Supabase)~~ | ~~—~~ |

---

## 🟠 ВЫСОКИЕ (исправить в ближайшее время)

### Баги
| # | Описание | Файл | Строки |
|---|----------|------|--------|
| ~~BUG-4~~ | ~~`parseFloat(r.gross_total).toFixed(2)` даёт `$NaN`~~ | ~~`js/business-panel.js`~~ | ~~67~~ |
| ~~BUG-5~~ | ~~`calculateDaysLeft` возвращает `NaN`~~ | ~~`js/dashboard-items.js`~~ | ~~96~~ |
| ~~BUG-6~~ | ~~`typeof turnstile` без `window.`~~ | ~~`js/register.js`~~ | ~~104, 180~~ |
| ~~BUG-7~~ | ~~`#sidebar-overlay` отсутствует в `dashboard.html`~~ | ~~`dashboard.html`~~ | ~~—~~ |
| ~~BUG-8~~ | ~~`scanFileInput.value = ''` без null-guard~~ | ~~`js/verify.js`~~ | ~~600~~ |
| ~~BUG-9~~ | ~~`resultDetails` без null-check~~ | ~~`js/verify.js`~~ | ~~206, 232, 263~~ |

### Архитектура
| # | Описание | Файл | Строки |
|---|----------|------|--------|
| ARCH-1 | Supabase-клиент создаётся в **5 разных файлах** — ротация ключа требует правки везде | `auth.js`, `dashboard-auth.js`, `register.js`, `script.js`, `verify.js` | — |
| ARCH-2 | `escapeHtml` определён **5 раз** с разным набором экранируемых символов — `'` и `/` не везде | `business-panel.js`, `dashboard-items.js`, `dashboard-notifications.js`, `receipts.js`, `security.js` | — |
| ARCH-3 | Toast-система скопирована в **4 CSS-файлах** (~200 строк каждый) | `auth.css`, `dashboard.css`, `business.css`, `verify.css` | — |
| ARCH-4 | `.bottom-nav-item` полностью продублирован в `dashboard.css` и `business.css` (~45 строк) | `dashboard.css` / `business.css` | — |
| ARCH-5 | Компонент `.item-card` + вложенные классы продублирован в 3 CSS-файлах | `style.css`, `dashboard.css`, `business.css` | — |

### Переводы
| # | Описание | Файл | Строки |
|---|----------|------|--------|
| I18N-1 | `verify_btn` используется в `business.html:102` но **отсутствует** в `business-lang.js` | `business-lang.js` | — |
| I18N-2 | `msg_success` (EN) отсутствует — только русская версия | `js/script.js` | 62 |
| I18N-3 | `security.js` возвращает сообщения только на русском без i18n | `js/security.js` | 121–138 |

### CSS
| # | Описание | Файл | Строки |
|---|----------|------|--------|
| CSS-1 | `.feature-list` — точный дубликат блока дважды подряд | `css/roadmap.css` | 129 + 136 |
| CSS-2 | `.modal-overlay.active .modal-content` — дубликат с разными значениями | `css/dashboard.css` | 580 + 604 |
| CSS-3 | `backdrop-filter` без `-webkit-` в 6 местах — не работает в Safari | `style.css:565`, `dashboard.css`, `business.css` | — |
| CSS-4 | z-index конфликт: `.navbar`(1000) = `.modal-overlay`(1000) = `.sidebar-header`(1000) | `style.css`, `dashboard.css` | — |
| CSS-5 | `.verify-logo .lang-toggle` никогда не сматчится — класс на `.verify-top-bar` | `css/verify.css` | 81–88 |
| CSS-6 | Мёртвый CSS: `.section-divider`, `.shop-badge`, `.form-message` — не используются в HTML | `dashboard.css`, `style.css` | — |

---

## 🟡 СРЕДНИЕ

| # | Описание | Файл | Строки |
|---|----------|------|--------|
| MED-1 | Мёртвый код: `window.renderNotifications` никогда не определён, но вызывается | `js/dashboard-items.js` | 63–65 |
| MED-2 | Мёртвый код: `pendingDeletePath` объявлен, присваивается, но никогда не читается | `js/receipts.js` | 4, 442 |
| MED-3 | `console.log` с эмодзи в production (`✅ Профиль сохранен успешно`) | `js/register.js` | 143, 157 |
| MED-4 | `unlockScroll` диспатчит синтетический `touchstart` — хак для iOS, может триггерить чужие обработчики | `js/dashboard-nav.js` | 22–24 |
| MED-5 | `restoreListeners()` навешивает обработчики заново при каждом рендере, старые не снимаются | `js/receipts.js` | 272–407 |
| MED-6 | `saveToggle` глотает ошибки молча — пустой `catch` | `js/dashboard-settings.js` | 152–161 |
| MED-7 | `setSafeHTML` с `allowHTML=true` — XSS-вектор при пользовательском вводе | `js/security.js` | 66–76 |
| MED-8 | QR-формат не экранирует `|` и `:` в значениях — ломает парсинг | `js/receipt-generator.js:74` / `js/verify.js:109` | — |
| MED-9 | `script.js` и `login-lang.js` используют `innerHTML` для переводов — XSS-вектор | `js/script.js:132`, `js/login-lang.js:127` | — |
| MED-10 | `toast.js` при `mouseleave` сбрасывает таймер на 1500ms вместо оставшегося времени | `js/toast.js` | 56 |
| MED-11 | `beforeunload` вызывает `stopCamera()` — модификация DOM при выгрузке страницы бессмысленна | `js/verify.js` | 682 |
| MED-12 | `jspdf` и `qrcode-generator` (347 KB) загружаются на каждой странице receipt, хотя нужны только по запросу | `receipts.html` | 16–17 |
| MED-13 | Все CDN-скрипты без SRI/integrity — supply-chain атака | Все HTML | — |

---

## 🟢 НИЗКИЕ

| # | Описание | Файл | Строки |
|---|----------|------|--------|
| LOW-1 | `lang="en"` на всех страницах с русским контентом (кроме `business.html` и `roadmap.html`) | все HTML | — |
| LOW-2 | Нет `<meta name="description">` на 8 из 9 страниц | все HTML | — |
| LOW-3 | `<a href="#">` как кнопки — не-semantic, нет `role="button"`, нет keyboard handler | `dashboard.html` | 72, 75, 79 |
| LOW-4 | `lang-toggle`, `theme-toggle` в сайдбаре — `<a>` вместо `<button>` | `dashboard.html` | 72, 75 |
| LOW-5 | `<img src="" alt="...">` в verify.html — пустой src делает запрос к текущей странице | `verify.html` | 91 |
| LOW-6 | `pendingDeletePath` присваивается `null`, но никогда не читается | `js/receipts.js` | 442 |
| LOW-7 | `let calendarCurrentMonth` и `let calendarCurrentYear` объявлены после return | `js/dashboard-items.js` | — |
| LOW-8 | Событие `lang-changed` не диспатчится на `roadmap.html` (нет `theme.js`) | `js/roadmap-lang.js` | — |
| LOW-9 | Cross-tab theme sync (через `storage` event) отсутствует на `business.html` | `business.html` | — |
| LOW-10 | `padding: 0px` сразу перезаписывается `padding: var(--space-8)` | `css/business.css` | 74–78 |
| LOW-11 | `0px` вместо `0` в box-shadow — мелочь | `dashboard.css` | 29–31 |
| LOW-12 | `--primary-color` не определён, используется fallback `var(--primary-color, #3b82f6)` | `style.css` | 1443 |
| LOW-13 | Hardcoded `#10b981`, `#f59e0b`, `#ef4444` вместо `var(--success)`, `var(--warning)`, `var(--error)` | десятки мест | — |
| LOW-14 | Потенциальная утечка `fa-undefined` при неизвестном типе тоста | `js/toast.js` | — |
| LOW-15 | `input-group:has([required]) label::after` — дубликат в 4 CSS-файлах | `style.css`, `auth.css`, `business.css`, `dashboard.css` | — |

---

## 📊 Статистика

| Приоритет | Новые | Было в прошлом аудите | Пофикшено |
|-----------|-------|----------------------|-----------|
| 🔴 Critical | 1 (BUG-1) | 3 (C1-C3) | 5 (BUG-1,2,3 + SEC-1,3 + SQL-1) |
| 🟠 High | 6 (BUG-4..9 + ARCH, CSS, I18N) | 7 (H1-H7) | 1 (H1) |
| 🟡 Medium | 5 (MED-1,2,3,11,12) | 10 (M1-M10) | 1 (M7) |
| 🟢 Low | 4 (LOW-8,9,10,14) | 8 (L1-L8) | 2 (L3, L5) |
| **Всего** | **15 новых** | **28 известных** | **11 пофикшено** |

**Итого активно: ~32 бага/проблемы**
